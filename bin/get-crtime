#!/bin/bash

inode=$(stat -c %i "$1")
fs=$(df  --output=source "$1"  | tail -1)
crtime=$(sudo debugfs -R 'stat <'"${inode}"'>' "${fs}" 2>/dev/null | 
grep -oP 'crtime.*--\s*\K.*')
printf "%s\n%s\n" "$1" "${crtime}"
